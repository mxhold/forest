{
  "scripts": {
    "check": "tsc",
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "esbuild": "^0.12.15",
    "typescript": "^4.3.5"
  }
}
