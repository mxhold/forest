export const CANVAS = {
  size: { width: 400, height: 300 },
};

export const GAME = {
  framesPerSecond: 30,
};

export const MOVEMENT = {
  stageDurationMs: 50,
};

export const ATTACK = {
  stageDurationMs: 50,
};
