export const CANVAS = {
  tileWidth: 20,
  size: { width: 400, height: 300 },
};

export const ENGINE = {
  framesPerSecond: 30,
};

export const MOVEMENT = {
  stageDurationMs: 50,
  wanderEveryMs: 3000,
};

export const ATTACK = {
  stageDurationMs: 50,
};
